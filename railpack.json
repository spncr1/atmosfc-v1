{
  "$schema": "https://schema.railpack.com",
  "provider": "python",
  "deploy": {
    "startCommand": "sh -c 'python -m alembic upgrade head && python -m uvicorn backend.main:app --host 0.0.0.0 --port ${PORT:-8000}'"
  }
}
